## needs: a-device  (1)
- [frontend] Has the phone test ever been run end to end? serve.py:43-52 says a LAN bind exercises layout but that signing in needs a Google Cloud Console redirect-URI registration that is not in this repo, and there is no artifact in the tree recording a device, a viewport or a result.
## needs: a-running-database  (18)
- [schema] Are there tables in the live database that no code creates any more — e.g. leftovers from migrate_scores.py --drop-columns, or an `ingest_progress` from the pre-split shared library? The code only tells me what WOULD be created on a fresh run.
- [schema] Does the `webapp` Postgres role actually hold the GRANTs schema_web.py:37-41 lists on jobs_app, or has an ensure_app_view DROP fallback already silently revoked them at some point? Nothing in the repo re-grants and nothing records when the view was last dropped.
- [schema] How many job_scores rows are currently unversioned (all four columns NULL) vs stale vs current? migrate_score_versions.py exists precisely to answer this and its output is not committed anywhere I read.
- [schema] Which of the ten migrations have actually been applied to the live database? Nothing records applied-migration state — there is no schema_migrations table and no runner, so the only evidence is the shape of the data.
- [stages] Which profiles are actually active in the deployed database, and what are their daily_narrative_budget values? score.py:587 and config/pursuit-criteria.json both assert that `pursuit` is the sole active profile with budget 0 (i.e. score.py writes nothing nightly), and score.py:1179-1181 says `tech` and `frontend` are paused and hold every score in the table. None of this is verifiable from code.
- [stages] Has any hn_whoishiring posting ever reached the 3-pass vote path in production? schema.py:320-327 says no as of the FACTS_VERSION=3 bump. A `SELECT extraction_passes, count(*) FROM job_facts GROUP BY 1` would settle it.
- [stages] Does the deployed backend/.env set JOBS_EXPECTED_MODEL, and to what? llm.py:41-44 says it should be set in the deployed .env, but the file is not in the repo and model_mismatch() is a no-op when unset — meaning a silently swapped model is possible if it is not.
- [stages] What is JOBS_MATCH_FLOOR in the deployed environment? schema.py:365 defaults it to 40 but reads it from env, so the floor that actually filters job_matches is not knowable from source.
- [stages] Are the four version columns on job_scores populated for any row today, or is every row still in the 'unversioned' bucket? score.py:511-512 says today's tombstones are all unversioned. Only `score.py --stale-report` against the live database answers it.
- [frontend] Is `gap_bridging_angle` in fact null on every live row for the `pursuit` profile, as ui.mjs:43-49 claims it was when the card styling was changed? The shipped fixture has it populated on 3 of 4 jobs, so the fixtures and the live database disagree about how the Today list actually looks. This decides whether Today renders fit stories or falls back to the posting's own `summary` on every card.
- [frontend] Does jobs_app still hold ~166 rows for `pursuit`? crawl.mjs:28-31 and saved.mjs:12-16 both size the crawl (2 requests, cap 8) on that figure, measured 2026-08-02. If the corpus grew, Saved silently truncates at 800 and cold detail links get slow.
- [webapp] Does `pursuit` actually have zero job_scores rows today (which is what makes derive_tracks() return [] and leaves builder_profiles.tracks NULL for every Builder)? The claim is only in a docstring.
- [webapp] Are the GRANTs described in REQUIRED_TABLES actually issued to the jobs_web role on the live database? verify_schema() would refuse to start if not, but nothing in a checkout proves the deployed state.
- [webapp] jobs.py's `q` filter is an ILIKE over title/company only, with no index named for it; is that acceptable at the current table size, or is it a sequential scan on every keystroke?
- [ingest] How many rows does each platform actually contribute today, and is any source at zero? I read the writers, not the table — every count in this brief is a claim about code paths, not about data. config/volume-floors.json's per-source numbers are the only measured volumes in the repo and they were derived from five nights of journalctl.
- [deploy] Does backend/scripts/backup-jobs.sh actually work here? It is referenced by two units, exists and is executable, but its docker-exec/pg_dump path and its behaviour without ~/.config/jobs-backup.env were not exercised — I was read-only and did not run it.
- [deploy] Which Postgres roles actually exist and what grants do they hold? jobs-api.service:17-24 makes a precise claim (jobs_api can do six things, no DELETE, no CONNECT to the events database) that cannot be checked from the tree.
- [evals] Was `evals label report` ever successfully run end-to-end against golden-v1.jsonl, or does it still refuse? With 2 labellers and 10 overlap postings the ceiling cells may be too thin for Interpretable.__post_init__ to accept on every field. I could not determine this without a database holding eval_labels.
## needs: an-external-account  (3)
- [api] Is the cloudflared ingress rule for the contributor API hostname actually live at Cloudflare? deploy/cloudflared/config.yml is a template with angle-bracketed placeholders and cloudflared is not running here, so the repo cannot tell me whether a hostname is currently pointed at a dead 8420.
- [deploy] Is /home/eric/.local/bin/hermes's `--to telegram` target still configured? The binary exists and the 2026-08-02 failure did trigger OnFailure, but whether the message was delivered is only visible in the journal of jobs-failure@1 instances or in Telegram.
- [evals] Does `tools/claude-ceiling-test.py` still parse claude-bench.py's current output format? The six regexes are coupled to print statements in another file and I did not run claude-bench.py (it makes live billed calls).
## needs: more-reading  (10)
- [schema] lib/http.py has no rate limiting or per-host politeness delay at all. Whether any caller implements its own throttle (ingest/ats.py mentions counting requests it issues) is outside the files I was assigned.
- [schema] Whether evals/labels.py's three tables are considered part of this schema's contract or an independent store — webapp/schema_web.py:715 calls their ensure_schema on the shared connection, but schema.py does not mention them.
- [frontend] Does anything validate frontend/fixtures/contract/ (17 files)? verify_fixtures.py reads only fixtures/shipped/ (verify_fixtures.py:64) and check_client.mjs only SHIPPED (check_client.mjs:39). The contract directory contains ASPIRATIONAL_* shapes the client deliberately does not implement, so it can drift arbitrarily with nothing going red.
- [api] Whether the `claim` endpoint's per-query metering survives a partial failure: try_claim_query commits inside pick_stale_queries_by_bucket (query_claims.py:279) BEFORE the log_submission loop runs (app.py:307-311), so a crash between them leaves a query locked for 15 minutes and unmetered. I could not construct that failure read-only to see whether it is reachable in practice.
- [ingest] Whether ORDER's fallthrough was cut deliberately or simply never wired. serp/__init__.py:73-75 says the six cut adapters are 'cut by not appearing here', which is about PROVIDERS, not about whether the two survivors chain. Nothing in the tests exercises a serpapi->apify fallthrough.
- [ingest] Whether ats.py's `--delta` path is ever used. It applies to SmartRecruiters only and disables closure, and nothing in run-daily.py passes it, so it may be a documented capability with no caller.
- [deploy] Is the drift between ~/.config/systemd/user/*.jobs* and deploy/systemd/ meant to be closed by symlinking (as deploy/README.md presumably instructs) or by copying? I did not open deploy/README.md — it was not in my assigned reading.
- [evals] Why do two runs of the same model at temperature 0 over the same frozen 120-row fixture five days apart produce agree2 differences of up to +9.6 points (remote_policy 81.7% → 91.3%)? Candidates I cannot separate from code: a silent provider-side model revision behind the `deepseek-v4-flash` label, a prompt change in extract.py between the two dates, or ordinary sampling noise inside overlapping Wilson intervals. The .md record asserts the third but does not rule out the first two.
- [evals] Do `metrics.bootstrap_delta()` and the whole two-orderings comparison path have any production caller, or is it built-ahead for task 30? I found the implementation and its tests but no invocation from __main__.py, report.py, labels.py or any tool.
- [evals] How much of the 111-entry replay cache is still valid? Entries are keyed on the prompt text, and extract.py's prompt has demonstrably changed (role_track was added). I could not determine the hit rate without running the harness, which would place live calls.
## needs: the-owner  (14)
- [stages] Does anything besides tools/volume-check.py consume .run-volumes.jsonl, and is that timer actually installed? run-daily.py:351-365 explicitly declines to check the floors itself, so 'the run that never happened' is only caught by machinery outside this repo's code.
- [frontend] Is the Contribute surface (task 24) still intended? app.mjs:107-113 leaves the recipe for it as the worked example for the sixth surface, but .claude/CLAUDE.md describes api/ — the contributor work queue — as expected to be deprecated. Those two point in opposite directions and only the owner can say which is live.
- [webapp] Does the deployed .env set SESSION_COOKIE_SECURE true or false? The key is present but I did not read values. If false in anything but local plain HTTP, the session cookie — the client's only credential — travels in the clear.
- [webapp] Is FRONTEND_ORIGIN in the live .env consistent with GOOGLE_REDIRECT_URI and ALLOWED_ORIGINS? The .env comments record that a mismatch previously made sign-in succeed and land the browser nowhere, with no error and no log line.
- [webapp] Is the mounted-static-files surface (GET /, frontend assets) part of the intended production topology, or only a local dev convenience? app.py's own docstring describes deployment as a separate manual step behind a reverse proxy.
- [api] Was `manage_users.py init-schema` deliberately not re-run after task 24 added submission_log.action (because the service is being deprecated), or is it simply an un-applied migration on a service nobody starts?
- [api] Has any API key ever been minted and handed to a person outside this database? `api_keys` is empty here, but a key minted on another machine against another database would not show up. I could not determine this from the tree.
- [api] Is the intent to retire api/ entirely (deleting the unit and the ingress rule together, as jobs-api.service:6-9 instructs) or to keep it warm? The unit says 'expected to be deprecated' and the code is actively maintained — tranche work landed in it on 2026-08-02 — which are opposite signals.
- [ingest] Is the duplication between ingest/google-serpapi.py + ingest/google-apify.py and serp/providers/* intended to be temporary (the two scripts eventually retired in favour of searchqueries.py) or permanent (the nightly query bank and the per-Builder search runner are two products)? The code supports both readings: serp/__init__.py frames the seam as filling run_due()'s missing callable only, but the provider docstrings frame themselves as lifts of the ingest code.
- [ingest] Does searchqueries.py's Ledger actually get exercised nightly, i.e. is SERPAPI_API_KEY set in backend/.env on this host? build_provider() returns (None, 'no credential') and dispatches nothing when it is not, and the whole serp/ path would then be dead in production while looking healthy.
- [deploy] Were jobs-webapp.service, jobs-api.service, cloudflared.service and the four monitoring timers ever installed and then removed, or have they never been installed since c4cff65 landed nine hours before this reading? The units are new today, so 'not yet installed' is the likely answer, but only the owner can confirm whether install was intended for this machine at all.
- [deploy] Which Cloudflare account, domain and tunnel UUID should fill the template, and do SESSION_COOKIE_SECURE / GOOGLE_REDIRECT_URI / FRONTEND_ORIGIN in webapp/.env already point at a real hostname? I read key NAMES only, per the brief, so I cannot say what the values are.
- [evals] Which of the two committed n=115 extract selfchecks is the current floor of record — the 2026-07-28 run that CLAUDE.md quotes, or the 2026-08-02 run that supersedes it and adds role_track? Both are committed, both are n=115 on the same frozen corpus, and neither JSON carries a supersession marker.
- [evals] Are `tools/compare-models.py` and `tools/claude-bench.py` still used, or superseded by `python3 -m evals run --model`? Both still do live first_seen DESC selection. Nothing in the code marks them deprecated, but evals/__init__.py describes the nine tools it replaced without naming which.