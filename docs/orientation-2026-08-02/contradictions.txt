# 37 facts a brief asserted that the code refuted

[suites] The brief's premise that 'several [modules] gate on a scratch database being available' and asks me to identify 'which test MODULES skipped entirely' does NOT hold in this environment: ZERO tests skipped in any of the three suites. The gate exists and 12 backend modules use it, but it evaluated TRUE here. Reproduce: `cd /home/eric/apps/jobs/backend && python3 -m unittest discover -s tests -v 2>&1 | grep -cE '\.\.\. skipped'` prints 0. Every hit from the brief's suggested grep ('skipped|skip', case-insensitive) is a test NAME containing the word 'skip' (e.g. test_lib_envfile.TestParse.test_unparseable_lines_are_skipped_not_fatal ... ok), not a skipped result. The suggested grep is a false-positive trap: it cannot distinguish a skipped test from a passing test about skipping. The reliable pattern is the literal result marker `... skipped`, or the `(skipped=N)` suffix that unittest appends to the OK line — neither appeared.

[suites] Consequence of the above: all three suites printed a bare `OK` with no `(skipped=N)` and no `(failures=N)`/`(errors=N)` suffix, so failures=0, errors=0, skipped=0 for all three. Those zeros are read off the absence of the suffix, not off an explicit counter line — unittest only prints the parenthetical when a count is non-zero.

[schema] The brief says `backend/lib/*.py` is `(dbconn, envfile, http, ids, state, text, timeparse, upsert, __init__)`. That is exactly what is there — no contradiction, noted only because it is the one enumerable claim in the brief. Everything else in the brief matched the code.

[schema] Minor, but the brief frames `add_missing_columns` as adding columns to schema.py's tables only. It also runs outside schema.py: `lib/state.py:56` and `lib/state.py:101` add `claimed_at` to the watermark table, and `webapp/schema_web.py:406` adds `app_users.prior_domain`. Enumerating only schema.py's four call sites would miss two tables.

[stages] The brief calls relevance.py one of 'the four pipeline stages' and asks what rows it reads and writes. It reads and writes NO rows. It is a pure SQL-fragment builder with no database access, no main(), no __main__ guard, and it is not one of run-daily.py's 14 steps (backend/relevance.py:1-335; backend/run-daily.py:124-241). The executable stages are three, not four: extract.py, match.py, score.py.

[stages] The brief asks whether run-daily's step list is 'the four stages plus ingest'. It is not. There are 14 steps: 1 ATS-token discovery (tools/ats-discover.py), 8 ingest scripts, searchqueries.py (ingest-shaped, dispatches metered SerpApi/Apify searches that write `jobs` rows), then extract/match/score, then cohort.py (folds job_events into cohort_signal). relevance.py is absent from the list entirely (backend/run-daily.py:124-241).

[stages] run-daily.py's own module docstring contradicts its own code: it says 'nine steps', 'all nine', 'the nine scripts listed in STEPS' and 'two of the nine steps make LLM calls' while STEPS holds 14 entries (backend/run-daily.py:15, 20, 34, 77, 292-294 vs 124-241). Nothing enforces the count; the tests that read STEPS check names and args, never length (backend/tests/test_score_versions.py:799-812, backend/tests/test_volume_floors.py:128-137).

[frontend] `.claude/CLAUDE.md` states, in the `frontend/` paragraph: "Today, Job detail and Saved are built; search, onboarding and the phone test are not". The code contradicts the first two of those three. `frontend/js/search.mjs` is 629 lines with both views implemented, wired into `app.mjs:127` and given a tab at `index.html:47`; `frontend/js/onboarding.mjs` is 648 lines with both sub-screens implemented, wired into `app.mjs:128-132` and given the "Profile" tab at `index.html:48`. Twenty of `check_client.mjs`'s 54 passing checks exercise those two screens by name. The commits that landed them are `9a774e1` ("26 — the onboarding screen") and `3c0452f` ("32 — the search screen"), both already on main. Only the third item, the phone test, is still partly open — `serve.py --host` (commit `55a7cb7`) makes the LAYOUT half exercisable, and `frontend/serve.py:43-52` says the sign-in half still needs a Google Cloud Console redirect-URI change that is not in this repo. Downstream tasks should not plan around search or onboarding being unbuilt.

[frontend] The task brief asks whether the client sends cookies with `credentials: 'include'`. It does not. Every fetch in the client uses `credentials: "same-origin"` — `frontend/js/api.mjs:74` for all thirteen JSON endpoints and `frontend/js/events.mjs:171` for the keepalive unload flush. This is a deliberate choice documented at `frontend/js/api.mjs:20-25`, and it is load-bearing: `api.mjs:27` sets `BASE = ""` so every request is a relative path, and `"same-origin"` would DROP the session cookie if the page were ever served from a host other than the API's. The cookie works today only because `frontend/serve.py:78` mounts the page onto the API process itself. Anyone who assumed `'include'` and moved the page to a second dev server would get silent logged-out behaviour with no error.

[git-a] Task 15 has no commit at all, and no code. The brief asked me to find commits for 01..29; there are none for 15. The task file docs/tasks/refactor/tranche_three/15-ingest-usajobs-adzuna.md was created by 28f1d0e and never acted on. The code itself records why: backend/tools/ats-discover.py:57-58 says 'Task 15 is blocked on an Adzuna app_id/app_key that requires registering an account', and adzuna_top_companies() at backend/tools/ats-discover.py:268 is an explicit STUB. Reproduce: `ls backend/ingest/*.py` (no usajobs/adzuna module) and `grep -rin 'adzuna\|usajobs' backend/` (3 files, all stubs/comments/seed data).

[git-a] The brief's suggested method `git log --oneline --grep='<N>'` is unreliable for single- and double-digit task numbers and will produce false attributions. `git log --grep='15'` returns 38 commits, NONE of which implement task 15 — they match on 'n=15', 'D15', dates and other numerals. Every attribution above was made by reading subjects and opening `git show --stat`, not by grep.

[git-a] Commit 5092568 (task 06, 2026-07-28 03:57) states in its body that 'Tasks 10, 11 and 13 are held pending a design decision' because task 06's stop-branch gate fired. Tasks 10 (7d94bb1, 09:15), 11 (da4942c, 13:57) and 13 (fa2d7a7, 2026-07-28) all landed the SAME DAY, within 5-10 hours. This is not a violation — the resolving decision is 943d899 (08:57) 'Selective majority-of-3 and a drain loop -- the two extraction decisions', which explicitly answers the gate with config/extraction-policy.json — but 943d899 carries no task number, so anyone reconstructing this from subjects alone will read 10/11/13 as having ignored a stop gate.

[git-a] Four tasks in the 01-29 range have commits whose diff touches ZERO code files: 02 (36d83f5), 05 (e4bddd3), 22 (66c9d18) and the first half of 23 (eee979d). For 02 (a register), 05 (a measurement) and 22 (a spike) that is the correct deliverable, so 'no code' is not evidence of no work here. Tasks 20 and 21 (0d8c7ee) are the genuine case: docs-only, and no implementation exists anywhere.

[git-a] Task 23 will read as 'descoped' to anyone who stops at its first commit. eee979d (2026-07-28) is titled 'build 23, descoped' and is docs-only; the actual provider seam landed five days later in 8fe0e57 (2026-08-02) as the entire backend/serp/ package plus an 837-line test module. The two commits give opposite answers about whether 23 was built.

[git-a] Two commits carry the byte-identical subject 'Point engagement-events.md's code_at at the commit that holds the code': 0a208ef (2026-08-01 19:36) and ed275d1 (2026-08-01 20:15). Each is a one-line change to the same file. Likewise 'Make the handoff current for a fresh session' appears twice (11f0fd0, 3d2acc1, plus d5897d8 on 07-28). Reproduce: `git log --format='%s' | sort | uniq -d`.

[git-a] Task 04's commit body (c3275be) says it 'corrects DEFECTS.md's own total: the register says 41 entries and contains D01-D42'. But the register as committed by task 02 already had 42: `git show 36d83f5:docs/ingest/DEFECTS.md | grep -cE '^#{2,4} +D[0-9]'` returns 42, ids D01..D42. The '41' 04 corrected was introduced between 36d83f5 and c3275be (e353e3e also edited DEFECTS.md). Minor, but it means the '41' figure in 04's body is not a statement about what 02 delivered.

[git-a] There is no `git revert` anywhere in this history — zero commits whose subject begins with 'Revert'. All the reversals in this repo are prose reversals inside forward commits, which means a mechanical revert scan finds nothing and under-reports what was undone.

[git-a] Git alone cannot tell you whether a task's Definition of Done was met — only that files changed. I did not open the task .md files (per the brief), so every 'landed' verdict above is 'code plausibly matching the task's name exists at HEAD', not 'the DoD was satisfied'. Tasks 07 and 23 are the two where that distinction is known to bite.

[webapp] The invariant as written in the code — backend/schema.py:617, 'they share only this file and lib/, and none imports another' — is only half true. webapp/ imports FOUR pipeline top-level modules beyond schema.py and lib/: `profiles` (onboarding.py:54, manage_app_users.py:63), `searchnorm` (search.py:55), `schema` (schema_web.py:29, search.py:54) and `evals.labels` (label.py:68, schema_web.py:156). The part that IS true: webapp imports nothing from api/, api imports nothing from webapp, and no pipeline module imports webapp (verified by grep across backend/*.py and backend/api/*.py). So 'the three processes do not import each other' holds between api/ and webapp/, but webapp/ does import the pipeline process's own modules, not merely the two shared ones.

[webapp] The brief says to report the port the service 'binds by default'. Strictly, the webapp binds nothing: config.PORT (backend/webapp/config.py:124) defaults to 8421 but is annotated 'Documentation only -- uvicorn takes --port', and app.py has no __main__ / uvicorn.run(). The only code that turns config.PORT into a bind is frontend/serve.py:162,177, which is in a different directory and a different entry point. 8421 is correct as an answer, but it comes from an env-var default plus a launcher, not from the service.

[api] The brief asks me to 'check deploy/ for a unit that references' contributor-worker/google-serpapi-worker.py. There is no such unit and the phrasing may imply one exists. deploy/systemd/jobs-api.service references the SERVER (uvicorn app:app on 8420) and never the worker; no unit, timer or shell script anywhere in the tree names the worker script. That is expected by design — the worker runs on volunteers' machines — but it is not what the question presupposes.

[api] The brief frames this as 'the contributor API process' as though it is a running process. It is not running and cannot currently start: app.verify_schema() raises 'public.submission_log: missing column(s) action' against the live database, nothing is listening on 8420, and deploy/systemd/jobs-api.service is not installed as a systemd unit (systemctl --user cat jobs-api.service → 'No files found'). I am reporting this rather than describing the service as operational.

[api] backend/api/app.py:29-31 documents 'RUN LOCALLY: uvicorn app:app --port 8420', but no module in api/ loads backend/api/.env (no envfile/dotenv call exists anywhere in the package). Run that way, DATABASE_URL is unset and query_claims.py:78-80's password-less fallback is used, which fails to authenticate. The documented local-run instruction is incomplete as written.

[ingest] The brief lists `backend/ratelimit.py` as part of the ingest area. It is not: ratelimit.py is client-side RPM/RPD limiting for LLM endpoints (LLM_MAX_RPM / LLM_MAX_RPD / LLM_QUOTA_STATE), and its only non-test importer is llm.py:29. No ingest script imports it, and no ingest HTTP call is rate-limited by it — ingest pacing is per-script constants (ats.py:247 REQUEST_DELAY_SECONDS=0.5, workday.py:218 WORKDAY_REQUEST_DELAY=1.5, weworkremotely.py:104=2.0, builtin-nyc.py:141 BUILTIN_DETAIL_DELAY=2.0, nyc-open-data.py:165). Verify with: cd /home/eric/apps/jobs && grep -rn 'import ratelimit' backend/ --include=*.py

[ingest] The brief frames the bare-`upsert()` three-tuple unpack as a live defect class and asks which ingest scripts still do it. NONE do. All nine writes in backend/ingest/ use upsert_checked (ats.py:1134, ats_sources.py:200, builtin-nyc.py:497, google-apify.py:259, google-serpapi.py:402, hn-hiring.py:508, nyc-open-data.py:688, weworkremotely.py:278, workday.py:1045), and `grep -rn '= upsert(' backend/ingest/` returns nothing. Two tests pin it by reading source text (tests/test_ats_new_platforms.py:657-664, tests/test_workday_ingest.py:571-578). The historical defect ('at least four ingest scripts') was closed in commit e353e3e, 'Stop discarding upsert errors, at all eight call sites (tranche_one/03)'.

[ingest] The brief's file list implies nine ingest scripts of equal standing, but ingest/ats_sources.py is not a script — it has no main() and no __main__ guard; it is the roster library imported by ingest/ats.py. Conversely, an ingest-shaped step the brief does not list, backend/searchqueries.py, IS in run-daily.py's STEPS (run-daily.py:197) and writes `jobs` rows through serp/ — it is the only caller of the provider seam the brief asks about.

[ingest] run-daily.py contradicts itself internally: its docstring says 'nine steps' in six places (lines 15, 18, 34, 77, 115, 260, 294) while STEPS holds 14 entries (lines 120-254). Runtime output is unaffected because the summary uses len(STEPS).

[deploy] The brief asserted no fact about the deploy area that the code contradicts. Two things it ASKED for do not exist in the form implied, and I flag them rather than invent answers: (1) 'what hostnames map to what local ports' — deploy/cloudflared/config.yml is an explicit TEMPLATE (config.yml:3-6); the hostnames are the literal placeholders `<webapp-hostname>` and `<contributor-api-hostname>` and the tunnel is `<TUNNEL-UUID>`, so no real hostname is mapped anywhere in this tree. (2) 'as what user' — no unit sets User= or Group=; they are systemd USER units run as the invoking user (cloudflared.service:4 states this deliberately), so the answer is 'whoever runs `systemctl --user`', which on this machine is eric.

[deploy] IN-TREE (not brief) CONTRADICTION worth surfacing: jobs-ingest.service:41 says 'Two of the nine steps make LLM calls' and backend/run-daily.py:77 says 'the nine scripts listed in STEPS', but STEPS currently holds FOURTEEN entries (tools/ats-discover.py, 8 ingest scripts, searchqueries.py, extract.py, match.py, score.py, cohort.py). The 2026-08-02 journal shows the version that ran printed '11/12 step(s) ok' — twelve. Three different counts, none current: the unit comment, the docstring, and the last run.

[deploy] IN-TREE CONTRADICTION: jobs-ingest.service:4-10 says the unit was 'BROUGHT IN-TREE BY TASK 33, VERBATIM from ~/.config/systemd/user/' to end the situation where 'the only copy of the schedule that runs this pipeline existed on one machine, outside version control'. That situation still holds: ~/.config/systemd/user/jobs-ingest.service is still a regular file dated 2026-07-26, not a symlink into the repo, and timers.target.wants/jobs-ingest.timer points at the machine-local copy. The tracked files are a backup of what runs, not the thing that runs.

[git-b] TASK 48'S OWN STATUS ROW WAS NEVER FLIPPED. HEAD (5cca001) is titled '48 — freeze the refactor at a known-green state', claims 'Three suites green on their own three interpreters, both doc checkers zero', and created the tag refactor-freeze-2026-08-02. But `git show 5cca001 -- docs/tasks/refactor/README.md` shows its ONLY register edit was to row 23; docs/tasks/refactor/README.md:170 still reads `| 48 | [Stop clean](tranche_nine/48-stop-clean.md) | a green, committed, known state to cut from | todo |` at HEAD. This is precisely the class of defect commit effea44 ('Correct two Phase 9 status rows the index still called todo') documented for tasks 36 and 40, and effea44's own message notes no check compares a task file's Status against its README row. I did not open 48-stop-clean.md (the brief forbids it), so I cannot say whether the task file's own Status line was updated.

[git-b] THE NUMBER 47 IS USED FOR TWO DIFFERENT TASKS AND ONLY ONE OF THEM IS IN THE REGISTER. docs/tasks/refactor/tranche_seven/47-widen-the-c4-match-body.md (created by 2586bb2, never touched again) and docs/tasks/refactor/tranche_eight/47-split-the-entry-point.md (created and landed by f0816ee) both exist at HEAD. `grep -nE '^\| 47 \|' docs/tasks/refactor/README.md` returns exactly one row, README.md:153, and it links tranche_eight. The tranche_seven task therefore has no row, no status, and no owner in the ordered index; it remains audit-docs C2-reachable only through docs/MEASUREMENT-TRAPS.md:193 and docs/tasks/refactor/DECISIONS.md:2680, which is why C2 is green (verified: `cd backend && python3 tools/audit-docs.py` prints 0 findings on C1–C7, exit 0). This is the same failure shape as the duplicate task 34 that 3f42e2d found, except that one was caused by a broken link and this one by number reuse across tranches.

[git-b] THE BRIEF SAYS 'there appear to be duplicate task files numbered 34'. Both files still exist at HEAD, but git shows the duplication was diagnosed and closed on 2026-07-31 by 3f42e2d: the two were merged, the orphan's dispositions table became §D of docs/tasks/refactor/34-documentation-cleanup.md (273 lines), and docs/tasks/refactor/tranche_six/34-documentation-cleanup.md was cut to a 32-line pointer stub whose heading reads '# 34 — Documentation cleanup — MOVED'. It is a DELIBERATE orphan that audit-docs.py C2 reports as ALLOWED with its reason printed (per 57c34a5's message). Flagging this only so the brief's framing is not carried forward as 'unresolved' — it is resolved, unlike the task 47 collision above.

[git-b] NO `git revert` COMMIT EXISTS ANYWHERE IN THIS REPO. `git log --grep='^Revert'` returns nothing across all 160 commits. Every reversal in this history is a prose-level disposition change inside a doc or a decision entry, never a git-level revert. If a downstream task expects to find reverted code by walking git, it will find none.

[evals] The brief asks 'what does --no-cache change?' as though it were a global flag. It is not. `--no-cache` exists ONLY on the `evals run` subcommand (__main__.py:574). `evals selfcheck` has no such flag — it has the INVERSE, an opt-in `--cache` that is OFF by default (__main__.py:598-600), plus a second belt-and-braces defence in runner.run_repeated where `use_cache=None` resolves to `repeat == 1` (runner.py:198-199). The CLAUDE.md phrasing '--no-cache only for cost/latency' is accurate for `run` but does not describe `selfcheck` at all.

[evals] The brief says 'go deep on ... backend/tools/relevance-report.py, audit-docs.py ...' and separately asks for `relevance-report.py --dead` output via `tail -30`. `tail -30` captures only the TIER 2 / TIER 3 sample blocks, which are selected with `ORDER BY random()` (relevance-report.py:92) and therefore differ on every invocation — I ran it twice and got two different sample sets with identical counts. The `--dead` result itself ('none -- every pattern matches at least one open title') appears ~40 lines ABOVE the tail. I have reported both, but a `tail -30` alone would not have captured the answer to the question asked.

[evals] Minor: the brief describes backend/evals/fixtures/ as 'the corpora'. It also contains three COMMITTED RESULT artifacts (selfcheck-labelled36-2026-08-02.json, run-labelled36-2026-08-02.jsonl, pursuit-criteria-goldens.json) and one HTML fetcher fixture (builtin-nyc-desync.html), i.e. it is not a corpus-only directory.